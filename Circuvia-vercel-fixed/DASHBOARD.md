# Circuvia Dashboard

`index.html` is now the dashboard/landing page.

Flow:
Dashboard → Open Simulator → `simulator.html`

The simulator retains the component library, SVG symbols, drag/drop placement, wiring, inspector, code and console.
